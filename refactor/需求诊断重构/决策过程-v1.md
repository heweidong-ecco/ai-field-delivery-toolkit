# 决策过程存档 v1

> grilling skill 会话对「① 需求诊断」的多轮决策压测记录。日期:2026-08-29。记录每个问题的拍板答案与补充意见,作为 `重构方案-v1.0.md` 的依据。

## 第一轮(靶子锁定)

- **靶子**:① 需求诊断(候选还包括 FDE 操作台整体、语义去重方案、数据准备管道默认行为)。用户选定 **① 需求诊断**。

| # | 问题 | 拍板 | 补充 |
| -- | ---- | ---- | ---- |
| Q1 | 诊断要驱动什么? | **C**:给客户看的正式文档 | — |
| Q2 | 5 维求和 25 分制成立吗? | 接受推荐(阈值是拍脑袋) | **无真实项目可校准,按时效性和最优解思考**;核心质疑:阈值的意义、分数代表什么 |
| Q3 | AI 和人工分歧凭什么人工赢? | 分歧即信息,保留不压平 | **关键动机**:人写需求时头脑里的内容比文本多 10 倍;单次 AI 打分无价值;增加人工打分机制;**需要"AI 对人工打分再次评分"的按键(当时没做),形成循环渐进剖析,应是 Agent loop 形式** |
| Q4 | 单次调用够不够稳? | 置信度模式是评判标准;兜底必须明显标注 | **真实链路**:生成详细需求报告 → 客户评估建议 → 上传客户返回的评估文件 → 生成二代报告 → 再次确认,循环 loop;简单 LLM 调用+打分无价值 |
| Q5 | 提示词自定义 vs 中立? | **C**:可改但保留强制中立条款,报告标注已修改 | — |
| Q6 | 兜底给估算分还是拒绝评估? | **折中**:方向信号(偏高/中/低)+ 明确标注非 AI | — |

**总结(用户)**:需求诊断模块要重构。

## 第二轮(重构方向)

| # | 问题 | 拍板 | 补充 |
| -- | ---- | ---- | ---- |
| Q7 | 不可替代价值是什么? | 推荐(a):**可审计的迭代交付**(版本化 + 留痕),输出形态为正式文档 | — |
| Q8 | Agent loop 用几种循环? | 推荐:三层分层(剖析/复核/版本)共用一份需求档案 | — |
| Q9 | AI 再评分输出什么、怎么收敛? | 推荐:逐维同意/修正+理由 + 偏置检测;不收敛保留分歧 | — |
| Q10 | 版本与历史存哪? | **a**:JSON 按 run_id(`tmp/web/diagnosis/<run_id>/`) | — |
| Q11 | 客户反馈文件怎么进循环? | 推荐(a):文本/PDF → LLM 提炼客户意见条目 → 追加 → 触发 v2(预留图片) | — |
| Q12 | 用什么支撑多轮调用? | 推荐:建 `core/llm.py` 统一客户端 | — |

## 第三轮(多 Agent 形态)

| # | 问题 | 拍板 | 补充 |
| -- | ---- | ---- | ---- |
| Q13 | 几个角色、职责怎么分? | 推荐:Generator / Critic / Reviewer 三角色 | **角色边界定义要严格、完整、全面,不是简单 prompt;Reviewer 审人工的结论和人工意见必须进报告定稿** |
| Q14 | 评审要不要盲审? | 推荐:盲审(独立打分后对比,分歧=问题信号) | — |
| Q15 | Critic 检查边界? | 推荐:需求支持度(硬标准)/一致性/过度自信/遗漏 | **一定要加入对抗式评审** |
| Q16 | LLM 预算封不封顶? | 推荐:封顶(~10 次),超预算人工接管 | — |
| Q17 | 复用 harness 还是独立编排? | **(b)** 诊断/ 内轻量管线式编排器 | **澄清:公共底座 core/llm.py 要建(一处建好,诊断/原型/飞轮都吃);"不要使用原来的"指不碰 prototype_assembler 的 ReAct harness,按需懒加载调用** |
| Q18 | 置信度怎么落地? | 推荐(c):置信度 = Generator 与 Critic 一致度 | **置信度很重要,是评判标准,应该足够高且有力度** |

## 第四轮(收尾收敛)

| # | 问题 | 拍板 | 补充 |
| -- | ---- | ---- | ---- |
| Q19 | v2/v3 整轮重跑还是增量? | 推荐:**增量重评**(只重评客户意见触及维度),报告带变更清单 | — |
| Q20 | 人工最终确认强制? | 推荐:**强制**(不确认不出正式报告) | — |
| Q21 | 一次建成还是分期? | 推荐:**分两期**(一期=多 Agent 对抗+复核+定稿单版;二期=版本循环+反馈+增量+档案) | — |
| Q22 | 档案 JSON 字段? | 推荐:requirement/prompt(+modified)/rounds[]/scores{4 角色}/divergences[]/confidence/versions[]/client_feedback[] | — |

## 未采纳/修正的意见(保留)

- 一期曾提议"人工复核 + AI 再评分"一次性给"AI 修正分"——被修正为**逐维同意/修正 + 偏置检测**,且 Reviewer 结论必须进定稿(Q13)。
- "公共底座"一度被我理解偏(以为不用建 core/llm.py),用户明确:**要建,且复用**;不用的只是 prototype_assembler harness(Q17 澄清)。
- 兜底原方案"给完整估算分"被修正为**只给方向信号 + 非 AI 标注**(Q6)。

## 后续行动

1. 实现一期(见 `重构方案-v1.0.md` §4)
2. 一期完成 → 更新本目录 README 状态 + 归档一期为历史版本
3. 二期(版本循环 + 客户反馈 + 增量重评 + 档案检索)
