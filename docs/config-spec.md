# 配置规范

## 配置来源优先级
1. 环境变量（最高优先级）
2. 本地配置文件 config.yaml
3. 默认配置（代码内置）

## 配置文件位置
- 全局配置：core/config/default.yaml
- 模块配置：各模块目录下 config.yaml

## 配置格式
- YAML 格式
- 所有模块通过统一配置入口读取
- 禁止在代码中硬编码配置

## 敏感配置
- API Key、数据库密码等通过环境变量传递
- 禁止写入配置文件

## 配置变更
- 配置变更需记录
- 生产环境配置与开发环境分离